<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ez Logs" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Tailwind CSS %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <% if user_signed_in? %>
      <!-- Navigation -->
      <nav class="bg-white shadow-sm border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <%= link_to root_path, class: "flex items-center" do %>
                  <h1 class="text-2xl font-bold text-gray-900">EZLogs</h1>
                <% end %>
              </div>
              <div class="hidden md:block ml-10">
                <div class="flex items-baseline space-x-4">
                  <%= link_to "Dashboard", root_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium #{current_page?(root_path) ? 'bg-gray-100 text-gray-900' : ''}" %>
                  <%= link_to "Events", events_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium #{current_page?(events_path) ? 'bg-gray-100 text-gray-900' : ''}" %>
                  <%= link_to "Teams", teams_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium #{controller_name == 'teams' ? 'bg-gray-100 text-gray-900' : ''}" %>
                  <%= link_to "Users", users_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium #{controller_name == 'users' ? 'bg-gray-100 text-gray-900' : ''}" %>
                  <%= link_to "Settings", settings_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium #{current_page?(settings_path) ? 'bg-gray-100 text-gray-900' : ''}" %>
                </div>
              </div>
            </div>
            <div class="flex items-center space-x-4">
              <div class="flex items-center space-x-2">
                <span class="text-sm text-gray-700">Welcome, <%= current_user.display_name %></span>
                <div class="w-2 h-2 bg-green-400 rounded-full"></div>
              </div>
              <div class="relative">
                <div class="flex items-center space-x-3">
                  <%= link_to "Profile", profile_path, class: "text-gray-600 hover:text-gray-900 text-sm" %>
                  <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "text-red-600 hover:text-red-500 text-sm" %>
                </div>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Mobile menu -->
        <div class="md:hidden">
          <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
            <%= link_to "Dashboard", root_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium #{current_page?(root_path) ? 'bg-gray-100 text-gray-900' : ''}" %>
            <%= link_to "Events", events_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium #{current_page?(events_path) ? 'bg-gray-100 text-gray-900' : ''}" %>
            <%= link_to "Teams", teams_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium #{controller_name == 'teams' ? 'bg-gray-100 text-gray-900' : ''}" %>
            <%= link_to "Users", users_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium #{controller_name == 'users' ? 'bg-gray-100 text-gray-900' : ''}" %>
            <%= link_to "Settings", settings_path, class: "text-gray-600 hover:text-gray-900 block px-3 py-2 rounded-md text-base font-medium #{current_page?(settings_path) ? 'bg-gray-100 text-gray-900' : ''}" %>
          </div>
        </div>
      </nav>
    <% end %>

    <main class="min-h-screen">
      <%= yield %>
    </main>
  </body>
</html>
